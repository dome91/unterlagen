package templates

templ Home(notifications []Notification) {
	@authenticatedLayout(notifications, PageHome) {
		<div class="flex items-center justify-center min-h-screen">
			<h1 class="text-3xl font-bold text-center">Welcome to Unterlagen</h1>
		</div>
	}
}
